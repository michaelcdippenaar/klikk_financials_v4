# External RAG Server

This document is a placeholder for when you connect to an **external** RAG server (e.g. pgEdge RAG Server, Ragie, or another service).

---

## Settings to connect

*(Fill in when you deploy an external RAG server.)*

| Setting | Description | Example |
|--------|-------------|--------|
| **Base URL** | Root URL of the RAG API (e.g. `http://host:port` or `https://rag.example.com`). | |
| **API key** | If the server requires authentication. | |
| **Collection / index name** | If the server uses named collections or indexes. | |

---

## What it is used for

*(Describe use cases, e.g. Postgres-backed RAG, Gmail ingestion, Planning Analytics context, or a second RAG for a specific domain.)*

---

## What it contains

*(Describe the server’s data model: collections, documents, vectors, metadata, and how they map to your apps or data sources.)*
